import { throttle, debounce, pauseEvent } from './function';
import { RenderableContent } from './react';
import { StringMap, Diff, Omit } from './types';

export { debounce, Diff, Omit, pauseEvent, RenderableContent, StringMap, throttle };
