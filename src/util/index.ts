import { throttle, debounce, pauseEvent } from './function';
import { RenderableContent, renderContent } from './react';
import { StringMap, Diff, Omit } from './types';
import { isIE } from './browser';

export { debounce, Diff, Omit, pauseEvent, RenderableContent, renderContent, StringMap, throttle, isIE };
